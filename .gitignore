# Dependencies
node_modules/

# Build output
dist/
out/
*.exe

# Runtime backups only — data/nkfms-data.json is tracked on purpose so the
# hisab can be carried between computers with git pull / git push.
backups/
data/backups/

# Logs
npm-debug.log*
*.log

# Editor / OS
.vscode/
.idea/
Thumbs.db
Desktop.ini
.DS_Store

# Supabase কী (anon key ব্রাউজারে যায়, তবু .env কমিট না করাই রীতি)
.env
.env.local

# Firebase Hosting
.firebase/
.firebaserc.bak
firebase-debug.log
